# AUTOMATION — ספר ההוראות לריצה האוטומטית (Routine: digest יומי + התראת שינוי)

> # 🛑 החוק העליון (מעל הכל) 🛑
> **אסור לשלוח/למסור שום חיזוי — בצ'אט, ב-digest, או בהתראה — ללא בדיקה מלאה.**
> "בדיקה מלאה" = כל 8 שלבי METHOD + מסד שחקן-שחקן מעודכן (בסיס + רענון 24ש'/1ש') לשתי הקבוצות
> + מאמן + פציעות/הרכב + גורם-הפתעה + H2H + שלב 7 (תהייה) + בדיקת-פלט (אחוזים=100).
> **אין "גרסה מהירה". אין קיצורי דרך. לחץ זמן אינו תירוץ.**
> אם אי-אפשר להריץ מלא — **אל תמסור מספר.** אמור: "טרם הרצתי מלא — לא אתן חיזוי עד שאשלים."
> כלל זה חל גם על ה-Routine וגם על כל תשובה ידנית בצ'אט. **כל פספוס של הכלל הזה = כשל.**

> **זהו ה-prompt שה-Routine מריצה כל שעה.** עצמאי ומפורש — ריצה אוטונומית בלי אישורי ביניים.
> prompt ה-Routine הוא רק: **"קרא את AUTOMATION.md בריפו ובצע אותו במלואו."**
> הענף הקנוני (גם default וגם מותר ל-push): **`claude/modest-wozniak-rrunlk`** — קרא ממנו וכתוב אליו (כך ה-state נשמר בין ריצות).

## המשימה
**מייל אחד ביום** (digest בוקר) עם **כל** משחקי 30 השעות הקרובות + **post-mortem** של אתמול.
בנוסף: ~שעה לפני כל משחק, אם ההרכב הסופי **משנה** את התחזית → התראת "שינוי" קצרה נפרדת.
> **שעות המשחקים:** ערב → בוקר מוקדם (🇮🇱). ביום הישראלי (08:00–18:00) **אין משחקים** → ריצות יום ריקות.

## לוגיקת החלטה (כל ריצה, לפי הסדר)

1. **ריצת הבוקר (פעם ביום, ריצה ראשונה עם שעה ישראל ≥ 07:00 שאין לה digest להיום):** עושה **שני דברים ברצף באותה ריצה**:
   - **א. Post-mortem:** שלוף בחיפוש web את **תוצאות אתמול**, השווה לתחזיות, **עדכן את `predictions/results-ledger.md`**
     (מאזן + לוג + כיול-ביטחון), וסווג כל החטאה:
     - **שונות (variance)** — toss-up שנפל לצד השני / הפתעה לגיטימית → **אין פעולה.** טעות ≠ כשל. אי-אפשר לנבא הכל.
     - **טעות מערכתית/דרסטית** — עיוורון סטטיסטי, **פספוס שחקן מרכזי שלא שיחק בקבוצה שגיבינו לנצח**, כלל שגוי, סיגנל שהתעלמנו ממנו → **עדכן את `METHOD.md`** באותה ריצה (commit) **לפני** ה-digest.
   - **ב. מסד דירוגים (חובה לפני הגזירה!):** לכל קבוצה במשחקי החלון — אסוף/רענן **מסד דירוגים עדכני מ-5 המקורות האיכותיים**
     (Transfermarkt · SofaScore · FBref/Opta · EA FC · FIFA) לכל **שחקן + שוער + מאמן**, **בדגש על עדכניות/כושר נוכחי**.
     שמור/עדכן ב-`data/team-ratings/<קבוצה>.md` (אם קיים — רענן הרכב/פציעות/כושר). גזור אינדקסי התקפה/הגנה → `engine/ratings.py` → λ → `engine/poisson.py`.
   - **ג. Digest:** הפק לפי METHOD + פלט הפואסון (EV-אופטימלי). פתח בכותרת `📊 מצב + לקח`. כל משחק עם צ'ק-ליסט פרמטרים מתקפל (כולל דירוגי הקבוצות, λ, P, EV).
2. **אימות שינוי (~1ש' לפני, ערב→בוקר):** לכל משחק ב-digest עם 0.75 ≤ HUK ≤ 2 — **רענן את הדירוג מההרכב הפותח הסופי**
   (מי באמת משחק) + פציעות רגע-אחרון → גזור λ מחדש דרך `ratings.py`/`poisson.py`. אם התוצאה **שונה** מה-digest → issue "שינוי". זהה → כלום.
3. **אחרת** (כולל כל ריצות היום 08:00–18:00) → צא בשקט.

> HUK = שעות עד הבעיטה. ה-post-mortem + digest רצים **יחד בריצת הבוקר** כדי שעדכון METHOD יחול מיד על תחזיות היום (אותו context).

## חקר הפרמטרים (לכל משחק)
לפי `predictions/_game-template.md` + `research/factors-and-coaches.md`, עם web search עדכני:
**פריור היסטורי** (`research/draw-probabilities.md` — מודאלי 1-0/2-1, תיקו לפי מחזור) · הרכב (צפוי/סופי) · פציעות וחסרים ·
כושר-כניסה (5 אחרונים) · מאמן וזהות טקטית · **H2H אחרון** (3-5 מפגשים — אינדיקציה) · **הטיית מארחת** ·
**סיגנל מגרש** (🏔️ גובה, 🔥 חום) · 🎲 **גורם הפתעה** (שוער/קרנות/מוטיבציה/שאננות יריבה) · משמעות לטבלה.

## גזירת תוצאה
**הרץ את מנוע הניבוי המלא ב-`METHOD.md`** (6 השלבים: Tier → xG בסיס → מודיפיקטורים → עיגול לסקור → ביטחון → שכבת pool).
שלב בתים = **בועז (כניסה יחידה)**. נוקאאוט = אסטרטגיית הפורטפוליו מ-`STRATEGY.md`. ציין ביטחון וסיגנל מגרש.

## STATE — איך יודעים מה כבר נשלח (חסין)
**מקור אמת ראשי: GitHub issues.** בתחילת כל ריצה:
- **digest:** בדוק אם כבר **נוצר היום** (created_at בתאריך ישראל של היום) issue שכותרתו מתחילה ב-`[תחזיות 24ש']`.
  אם כן → אל תיצור digest שני. **אל תסתמך על substring של תאריך בכותרת** (issue ישן עלול להכיל את תאריך היום ולחסום בטעות).
- **שינוי:** חפש issue `[⚠️ שינוי]` עם שם המשחק. קיים → דלג על האימות שלו.
**ה-issues הם מקור האמת הבלעדי** — אל תסתמך על מצב ה-git branch.

## התראה — ערוץ ראשי: GitHub issue (שורות נקיות — **לא טבלה**!)
**GitHub לא מציג טבלאות במייל.** לכן הגוף = **שורות קצרות עם תוויות אמוג'י**, כולל **תאריך ושעון ישראל**.
כל שורה מסתיימת ב-`<br>`.

- **Digest יומי:** כותרת `[תחזיות 24ש'] DD.MM — N משחקים`. גוף = כותרת + **בלוק לכל משחק** (מסודרים לפי שעה).
  כל משחק חייב לציין את **הבית** (שלב הבתים) או את **שלב הנוקאאוט** (1/16, 1/8, רבע, חצי, מק'3, גמר),
  **ואת ערוץ השידור המדויק בישראל לאותו משחק** — חפש **פר-משחק** בלוח השידורים (kan.org.il/sport5 לרוב חוסמים אוטומציה →
  השתמש ב-mako, walla מונדיאל, israelhayom, sportour, או חיפוש "[שם המשחק] שידור ישראל ערוץ"). ציין את הערוץ הספציפי
  (כאן 11 / כאן BOX / ספורט 1 / ספורט 2 וכו'). ברירת מחדל אם לא נמצא: "ספורט 1 / כאן BOX". **התוצאה תמיד בין שמות הקבוצות:**
  **מבנה חובה (קריאוּת!):** (1) שורת מאזן קצרה; (2) **בלוק "🎯 להזין היום" בראש — רשימה ממוספרת נקייה,
  שורה אחת למשחק** (משחק בסדר-האתר · התוצאה להזנה · מי מנצח · שעה · צבע) — **זה מה שהמשתמש קורא קודם**;
  (3) רק *אחרי זה* הפירוט המלא לכל משחק (אחוזים + צ'ק-ליסט מתקפל). הסיכום למעלה חייב להספיק לבד.
  ```
  📊 מאזן: X/Y · אתמול: [קצר מאוד].

  🎯 **להזין היום** (בועז (כניסה יחידה)):
  1. ספרד 2–0 כף ורדה · ספרד מנצחת · 19:00 · 🟢
  2. בלגיה 2–1 מצרים · בלגיה · 22:00 · 🟡
  3. ערב הסעודית 0–1 אורוגוואי · אורוגוואי · 01:00 · 🟡
  4. איראן 1–0 ניו זילנד · איראן · 04:00 · 🟡
  ⬆️ זה כל מה שצריך להזין. פירוט למטה רק אם רוצים ↓
  ───────────────
  ⚽ ספרד 2–0 כף ורדה · בית H · 19:00 🇮🇱 · 📺 כאן 11
  🎲 ספרד 84% · תיקו 12% · כף ורדה 4% (2–0: 14%)
  <details><summary>🧮 פרטים</summary>
  • Tier ESP S1/CPV S5 → GAP4 · λ 2.9/0.54 · de la Fuente cs+0.3 · EV 2–0(1.20) · תהייה: סיכון כמות-שערים, לא מנצח
  </details>
  (... שאר המשחקים באותו מבנה ...)
  📄 בועז (כניסה יחידה) · פירוט: predictions/auto/YYYY-MM-DD.md
  ```
- **שינוי (~1ש'):** כותרת `[⚠️ שינוי] משחק · HH:MM 🇮🇱`. גוף: `🔄 **משחק**: היה X → עכשיו **Y**<br>`
  + צ'ק-ליסט מתקפל `<details><summary>🧮 איזה פרמטר זז</summary>` שמראה **את הפרמטר/ים שהשתנו** (הרכב סופי/פציעה/מזג אוויר)
  ואיך הם הזיזו את החישוב (לפני → אחרי). שאר הפרמטרים שלא זזו — לציין "ללא שינוי".

## פלט
קובץ יומי `predictions/auto/YYYY-MM-DD.md`. לכל משחק בלוק עם תגית סטטוס (`[DRAFT]` / `[FINAL — אושר]` / `[FINAL — שונה: X→Y]`),
**שעת בעיטה בשעון ישראל + UTC**, וטבלת נעילה מוכנה להעתקה ל-footattack.

## Commit
שמור את הקובץ היומי ועשה commit לענף העבודה של הריצה (**ארכיון בלבד**). אל תרדוף אחרי ענף קבוע —
ה-STATE וההתראות הם **GitHub issues**, לא ה-git. אם ה-push לא מצליח לענף מסוים — זה לא כשל, המשך.

## גארדריילים
- **חוק הפלט-המלא (ברזל, גם בלחץ זמן!):** כל ניבוי חייב לעבור את **כל** 8 השלבים + מסד שחקן-שחקן (5 מקורות, 2 הקבוצות)
  + פציעות/הרכב סופי + גורם-הפתעה + H2H + שלב 7 (תהייה) + בדיקת-פלט. **אסור "גרסה מהירה".** אם אין זמן לרוץ מלא →
  אמור במפורש "טרם הרצתי מלא, זו הערכה חלקית" ואל תציג אותה כפלט סופי. **לחץ זמן אינו תירוץ לקצר את המודל.**
- **חוק בדיקת-הפלט (ברזל):** ודא **כל** מספר לפני מסירה. אחוזי תרחיש **חייבים להסתכם ל-100** (השתמש ב-`round_to_100`);
  λ הגיוני (0.2–3.6); EV ≥ 0; אין סתירות פנימיות. אל תמסור פלט בלי בדיקת שפיות. טעות חישובית = פוסלת את כל הניבוי.
- **חוק השעון החי:** לפני כל תשובה על "מה הקרוב / מה משוחק עכשיו / מה התוצאה" — **שלוף קודם שעה ותאריך אמיתיים**
  (`date -u` בשורת הפקודה, או חיפוש web "current UTC time"; המר לשעון ישראל = UTC+3). **אסור להניח שעה מהזיכרון.**
  אם עדיין לא ברור איזה משחק חי — שאל "מה משוחק עכשיו?" במקום לנחש.
- **חוק הברזל: אין תוצאה בלי חישוב מלא.** כל תוצאה שנמסרת לבועז (digest, שינוי, או תשובה בצ'אט) חייבת
  לעבור באותה ריצה: חקר web עדכני של כל 7 הפרמטרים + הרצת METHOD מספרית מפורשת. אסור למחזר תחזית
  ישנה/מקדימה ולהציג אותה כתחזית. אם אין זמן/נתונים לחישוב מלא — אל תמסור תוצאה; כתוב "טרם חושב".
- חקר אמיתי בלבד; אין נתון → הורד ביטחון. אל תמציא הרכבים.
- בידול לעולם לא אקראי — רק עם סיגנל שתומך *וגם* מבדל (ראה `STRATEGY-edge-vs-ai.md`).
- אל תזין ל-footattack (אין API; בועז מזין ידנית).

---

## הוראות הקמה (פעם אחת, ב-UI)
ראה הסבר צעד-צעד בצ'אט. בקצרה: claude.ai/code/routines → New routine →
Prompt = `קרא את AUTOMATION.md בריפו ובצע אותו במלואו.` · Repo = `BumbleBeeDream/Mondial` ·
Network = **Full** · Permissions = אפשר push ל-`claude/modest-wozniak-rrunlk` · Connectors = Gmail מחובר.

> **תזמון מומלץ (חיסכון במכסה):** המשחקים ערב→בוקר 🇮🇱; ביום הישראלי אין משחקים. הגדר **cron מותאם**:
> **`0 15-23,0-5 * * *`** (UTC) — רץ כל שעה מ-**18:00 🇮🇱 (ערב)** דרך הלילה עד **08:00 🇮🇱 (בוקר)**.
> כך נתפסים: **ריצת הבוקר 07:00 🇮🇱** (= 04:00 UTC, ל-post-mortem+digest) + **כל חלונות ה-1ש'** של משחקי הערב-לילה.
> מדולגות: 09:00–17:00 🇮🇱 (06:00–14:00 UTC) — אין משם משחקים, חוסך ~9 ריצות/יום. (ב-UI: Schedule → Custom → הדבק cron.)
